services:
  tests:
    build: .
    environment:
      SELENIUM_REMOTE_URL: http://selenium:4444/wd/hub
      TEST_GROUP: regression
    depends_on:
      - selenium
    volumes:
      - ./allure-results:/app/allure-results

